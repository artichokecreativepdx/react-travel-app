import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.PlanItAdventures.app',
  appName: 'react-travel-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
